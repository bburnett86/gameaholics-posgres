<div class="errors-container">
  <h1 class="errors-header">ERRORS:</h1>
  <% if flash[:errors] %>
    <ol class="errors-list">
      <% flash[:errors].uniq.each do |error| %>
        <li class="error-list-item"><%= error %></li>
      <% end %>
    </ol>
  <% end %>
</div>
